# DNS Setup Guide for staygoch.com
## เอกสารการตั้งค่า DNS สำหรับ GitHub Pages

เว็บไซต์ STAYGO ใช้ custom domain: **staygoch.com**

---

## 🌐 ขั้นตอนการตั้งค่า DNS

### ขั้นตอนที่ 1: ตรวจสอบว่าซื้อ Domain จากที่ไหน

domain `staygoch.com` ซื้อจากที่ไหน?
- Namecheap
- GoDaddy
- Google Domains
- Cloudflare
- อื่นๆ

ให้เข้าไปที่ dashboard ของ domain registrar นั้น

---

### ขั้นตอนที่ 2: ตั้งค่า DNS Records

ต้องเพิ่ม DNS Records ทั้งหมดนี้:

#### 🔵 A Records (สำหรับ root domain)

เพิ่ม **4 A Records** ชี้ไปที่ GitHub Pages servers:

| Type | Host/Name | Value/IP Address | TTL |
|------|-----------|------------------|-----|
| A | @ | 185.199.108.153 | 3600 |
| A | @ | 185.199.109.153 | 3600 |
| A | @ | 185.199.110.153 | 3600 |
| A | @ | 185.199.111.153 | 3600 |

**หมายเหตุ:**
- `@` หมายถึง root domain (staygoch.com)
- บาง registrar ใช้ `@`, บางที่เว้นว่าง, บางที่ใส่ชื่อ domain เต็ม
- TTL เป็นวินาที (3600 = 1 ชั่วโมง)

#### 🔵 CNAME Record (สำหรับ www subdomain)

เพิ่ม **1 CNAME Record**:

| Type | Host/Name | Value/Points to | TTL |
|------|-----------|-----------------|-----|
| CNAME | www | YOUR_USERNAME.github.io. | 3600 |

**⚠️ สำคัญ:**
- แทนที่ `YOUR_USERNAME` ด้วย GitHub username จริง
- ต้องมีจุด (`.`) ท้าย `github.io.`
- CNAME จะทำให้ `www.staygoch.com` ทำงานได้ด้วย

---

## 📋 ตัวอย่างการตั้งค่าตาม Registrar

### 🔸 Namecheap

1. เข้า Namecheap Dashboard
2. ไปที่ **Domain List** → เลือก `staygoch.com`
3. คลิก **Manage** → เลือกแท็บ **Advanced DNS**
4. เพิ่ม Records:

```
Type: A Record
Host: @
Value: 185.199.108.153
TTL: Automatic

Type: A Record
Host: @
Value: 185.199.109.153
TTL: Automatic

Type: A Record
Host: @
Value: 185.199.110.153
TTL: Automatic

Type: A Record
Host: @
Value: 185.199.111.153
TTL: Automatic

Type: CNAME Record
Host: www
Value: YOUR_USERNAME.github.io.
TTL: Automatic
```

5. **ลบ** CNAME Record เดิมถ้ามี (เช่น parkingpage)

---

### 🔸 GoDaddy

1. เข้า GoDaddy Dashboard
2. ไปที่ **My Products** → **DNS**
3. เลือก `staygoch.com`
4. คลิก **Add** แล้วเพิ่ม Records:

```
Type: A
Name: @
Value: 185.199.108.153
TTL: 1 Hour

(เพิ่ม A records อีก 3 ตัวด้วย IPs ข้างบน)

Type: CNAME
Name: www
Value: YOUR_USERNAME.github.io
TTL: 1 Hour
```

---

### 🔸 Cloudflare (แนะนำ - ได้ CDN ฟรี)

ถ้าใช้ Cloudflare เป็น DNS provider:

1. เข้า Cloudflare Dashboard
2. เลือก `staygoch.com`
3. ไปที่ **DNS** → **Records**
4. เพิ่ม Records (เหมือนข้างบน)
5. ✅ เปิด **Proxy status** (สีส้ม/cloud icon) สำหรับทั้ง A และ CNAME
   - จะได้ CDN ฟรี + DDoS protection

**Cloudflare Settings:**
- SSL/TLS: ตั้งเป็น **Full** (not Flexible)
- Always Use HTTPS: ✅ เปิด
- Auto Minify: ✅ เปิด (JS, CSS, HTML)

---

## 🔍 ตรวจสอบว่า DNS ตั้งค่าถูกต้อง

### ใช้เครื่องมือออนไลน์:

1. **DNS Checker**: https://dnschecker.org
   - ใส่ `staygoch.com`
   - ดูว่า A records ชี้ไปที่ GitHub IPs

2. **What's My DNS**: https://www.whatsmydns.net
   - เช็คว่า DNS propagate ไปทั่วโลกหรือยัง

### ใช้ Terminal (Mac/Linux):

```bash
# เช็ค A records
dig staygoch.com

# ควรเห็น:
# staygoch.com.  3600  IN  A  185.199.108.153
# staygoch.com.  3600  IN  A  185.199.109.153
# staygoch.com.  3600  IN  A  185.199.110.153
# staygoch.com.  3600  IN  A  185.199.111.153

# เช็ค CNAME สำหรับ www
dig www.staygoch.com

# ควรเห็น:
# www.staygoch.com.  3600  IN  CNAME  YOUR_USERNAME.github.io.
```

### ใช้ Command Prompt (Windows):

```cmd
nslookup staygoch.com
nslookup www.staygoch.com
```

---

## ⏱️ เวลาที่ใช้

- **DNS Propagation**: 15 นาที - 24 ชั่วโมง
- **GitHub SSL Certificate**: 5-10 นาที (หลัง DNS ทำงาน)
- **โดยปกติ**: ประมาณ 1-2 ชั่วโมง ก็ใช้งานได้แล้ว

---

## ✅ Checklist

- [ ] เพิ่ม 4 A Records ชี้ไป GitHub Pages IPs
- [ ] เพิ่ม 1 CNAME Record สำหรับ www
- [ ] ลบ DNS records เก่าที่ขัดแย้ง (ถ้ามี)
- [ ] รอ DNS propagate (15 นาที - 24 ชั่วโมง)
- [ ] ตรวจสอบด้วย dig/nslookup
- [ ] เปิด staygoch.com ใน browser
- [ ] ตรวจสอบว่า HTTPS ทำงาน (🔒)
- [ ] ทดสอบทุกหน้า (games, tools)

---

## 🐛 Troubleshooting

### ปัญหา: DNS ไม่ทำงาน

**สาเหตุ:**
- ยังไม่ propagate
- TTL สูงเกินไป
- มี records เก่าขัดแย้ง

**วิธีแก้:**
1. รอ DNS propagate (max 24 ชม.)
2. ลด TTL เป็น 3600 หรือ 300
3. ลบ CNAME/A records เก่าทั้งหมด
4. Clear DNS cache ในเครื่อง:
   ```bash
   # Mac
   sudo dscacheutil -flushcache
   sudo killall -HUP mDNSResponder
   
   # Windows
   ipconfig /flushdns
   ```

### ปัญหา: HTTPS ไม่ทำงาน

**สาเหตุ:**
- GitHub ยังไม่ issue SSL certificate
- DNS ยังไม่ propagate

**วิธีแก้:**
1. รอให้ DNS propagate ก่อน
2. ไปที่ GitHub Settings → Pages
3. Uncheck แล้ว check **Enforce HTTPS** อีกครั้ง
4. รอ 5-10 นาที

### ปัญหา: www ไม่ทำงาน

**สาเหตุ:**
- ไม่มี CNAME record สำหรับ www

**วิธีแก้:**
- เพิ่ม CNAME record: `www` → `YOUR_USERNAME.github.io.`

### ปัญหา: เว็บโหลดช้า

**แนะนำ:**
- ใช้ Cloudflare เป็น DNS provider
- จะได้ CDN ฟรี ทำให้โหลดเร็วขึ้น

---

## 📞 ต้องการความช่วยเหลือ

ถ้ามีปัญหา:
1. ดูที่ GitHub Settings → Pages → Check custom domain status
2. ดู error ที่แสดง (ถ้ามี)
3. เช็ค DNS ด้วย dnschecker.org
4. รอ 24 ชั่วโมง ถ้ายังไม่ได้

---

## 🎯 สรุป

หลังตั้งค่า DNS เสร็จ:
- ✅ `staygoch.com` → ใช้งานได้
- ✅ `www.staygoch.com` → redirect ไป `staygoch.com`
- ✅ HTTPS ทำงาน (🔒)
- ✅ เว็บโหลดเร็ว
- ✅ SEO friendly

**เว็บพร้อมใช้งานที่: https://staygoch.com** 🎉

